import * as Styled from './Logo.styled';

const Logo = () => {
  return (
    <Styled.LogoWraper>
      <Styled.InCode>InCode</Styled.InCode>
      <Styled.Finance>Finance</Styled.Finance>
    </Styled.LogoWraper>
  );
};

export default Logo;
